#include <stdio.h>

int main() {
    char name[20];
        printf("Digite o seu nome: ");
        scanf("%s", name);
        printf("Vai se fuder %s\n", name);

system("pause");
        
return 0;
}
