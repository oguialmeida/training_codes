// O código deve ser executado no terminal
// O programa repetira o que for digitado
import 'dart:io';

void main() {
  // Área da circunferencia = PI * (raio * raio)
  var texto = stdin.readLineSync();
  print("the value typed is " + texto.toString());
}
